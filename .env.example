VITE_API_URL=http://localhost:3000

# On Netlify: set VITE_API_URL as a site environment variable (Site configuration ->
# Environment variables) to your Render backend URL, e.g. https://5quadleague-backend.onrender.com
# Vite bakes this in at build time, so it must be set before the build runs, not after.
